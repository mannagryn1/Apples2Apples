run the program by being in map Apples2Apples and typing "java .\src\main\main.java". This will run the program with a host and 3 bots.

To run with "online" client, have one terminal act as host and run program with "java .\src\main\main.java x" where x is the number of desired online players. In as many different terminals as desired, run the program with an IP address as argument.
